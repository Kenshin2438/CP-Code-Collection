#include <bits/stdc++.h>
using namespace std;

using ll = long long;
#define all(a) begin(a), end(a)

void solve() {
  int a, b;
  // cin >> a >> b;
}

int main() {
#ifdef LOCAL
  clock_t start = clock();
#endif
  cin.tie(nullptr)->sync_with_stdio(false);
  cout << fixed << setprecision(10);
  int T = 1;
  // cin >> T;
  while (T--) solve();
#ifdef LOCAL
  clock_t end = clock();
  cerr << "\n" << (double)(end - start) / CLOCKS_PER_SEC << "ms\n" << endl;
#endif
  return 0;
}