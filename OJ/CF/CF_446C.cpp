#include <bits/stdc++.h>
using namespace std;

using ll = long long;
#define all(a) begin(a), end(a)

#ifdef LOCAL
#include "debug.hpp"
#else
#define debug(...) 42
#endif

const int mod = 1e9 + 9;
const int N = 3e5 + 10;

#define ld nd << 1 | 0
#define rd nd << 1 | 1
int n, m, a[N];

void solve() {
  cin >> n >> m;
  for (int i = 1; i <= n; i++) cin >> a[i];

  
}

int main() {
  cin.tie(nullptr)->sync_with_stdio(false);

  int T = 1;
  // cin >> T;
  while (T--) solve();

  return 0;
}
